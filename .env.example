# Application Settings
APP_ENV=development
# Set to 'production' on your hosting server to hide errors

# Database Settings
DB_HOST=localhost
DB_DATABASE=spk_hipertensipredictor
DB_USERNAME=root
DB_PASSWORD=

# Note: If you are using shared hosting (cPanel), 
# you usually need to create the database and user manually first, 
# then fill in the details above.
